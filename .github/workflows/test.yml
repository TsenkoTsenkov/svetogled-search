name: Tests

# Runs on every push/PR that touches the app or its markup. These tests do NOT
# need Meilisearch running — they validate the served HTML contract (favicon
# tags, no cache-busters, minifier sanity), that referenced static assets
# exist, and that the редакция editor puts edited words back on their original
# timestamps. Server/index-dependent tests self-SKIP when Meili isn't up.

on:
  push:
    branches: [main]
    paths:
      - 'search_app.py'
      - 'index.html'
      - 'static/**'
      - 'index_to_meili.py'
      - 'scripts/generate_favicons.py'
      - 'scripts/generate_themes.py'
      - 'theme_scoring.py'
      - 'redaction_align.py'
      - 'redaction_app.py'
      - 'redaction.html'
      - 'test_search.py'
      - 'test_redaction.py'
      - 'test_vim_motions.js'
  pull_request:
    paths:
      - 'search_app.py'
      - 'index.html'
      - 'static/**'
      - 'index_to_meili.py'
      - 'scripts/generate_favicons.py'
      - 'scripts/generate_themes.py'
      - 'theme_scoring.py'
      - 'redaction_align.py'
      - 'redaction_app.py'
      - 'redaction.html'
      - 'test_search.py'
      - 'test_redaction.py'
      - 'test_vim_motions.js'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install meilisearch pillow

      - name: Run tests
        run: python test_search.py

      - name: Run редакция + theme scoring tests
        run: python test_redaction.py

      # The vim motions of the editor are plain functions over strings, so
      # they run under node — no browser needed.
      - name: Run vim motion tests
        run: node test_vim_motions.js
